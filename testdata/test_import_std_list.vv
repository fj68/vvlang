import list from 'std/list.vv'
import console from 'std/console.vv'

let xs = [1, 2, 3]

fun incr(_, x)
    return x + 1
end

let ys = list.map(xs, incr)

if not(ys[0] == 2)
    console.print('fail')
end

console.print(ys[0])
